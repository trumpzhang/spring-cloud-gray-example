server:
  port: 7100

spring:
  application:
    name: order-app

management:
  endpoints:
    web:
      exposure:
        include: ["prometheus","health"]
  metrics:
    tags:
      application: BookManager
      percentiles-histogram:
        http:
          server:
            requests: true

